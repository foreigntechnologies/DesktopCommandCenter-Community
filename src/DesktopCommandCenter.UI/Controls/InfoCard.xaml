<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Name="Root"
    x:Class="DesktopCommandCenter.UI.Controls.InfoCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="16">
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,0,0,12">
                <FontIcon Glyph="{Binding IconGlyph, ElementName=Root}" FontSize="16" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                <TextBlock Text="{Binding Title, ElementName=Root}" FontWeight="SemiBold" FontSize="14" />
            </StackPanel>
            
            <ContentPresenter Grid.Row="1" Content="{Binding InnerContent, ElementName=Root}" />
        </Grid>
    </Border>
</UserControl>
