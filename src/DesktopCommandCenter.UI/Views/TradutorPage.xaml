<Page
    x:Class="DesktopCommandCenter.UI.Views.TradutorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="Transparent">
    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Tradutor" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,16"/>

        <Grid Grid.Row="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <Grid ColumnSpacing="16" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                
                <ComboBox ItemsSource="{x:Bind ViewModel.SourceLanguages, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.SourceLanguage, Mode=TwoWay}" HorizontalAlignment="Stretch"/>

                <Button Grid.Column="1" Content="⇄" Command="{x:Bind ViewModel.SwapLanguagesCommand}" Background="Transparent" BorderThickness="0" FontSize="20"/>

                <ComboBox Grid.Column="2" ItemsSource="{x:Bind ViewModel.TargetLanguages, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.TargetLanguage, Mode=TwoWay}" HorizontalAlignment="Stretch"/>
            </Grid>

            <Grid Grid.Row="1" ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                
                <TextBox Text="{x:Bind ViewModel.SourceText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" PlaceholderText="Digite ou cole o texto aqui..." MinHeight="300"/>
                <TextBox Grid.Column="1" Text="{x:Bind ViewModel.TranslatedText, Mode=OneWay}" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" MinHeight="300" />
            </Grid>
        </Grid>
        
        <Button Grid.Row="1" Content="Traduzir Texto" Command="{x:Bind ViewModel.TranslateCommand}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="24" Style="{StaticResource AccentButtonStyle}" MinWidth="150" />
    </Grid>
</Page>
