<Page
    x:Class="DesktopCommandCenter.UI.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:DesktopCommandCenter.UI.ViewModels"
    mc:Ignorable="d">

    <Grid Padding="24">
        <StackPanel Spacing="16">
            <TextBlock Text="{x:Bind ViewModel.WelcomeMessage}" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock Text="{x:Bind ViewModel.CurrentTime}" Style="{StaticResource HeaderTextBlockStyle}" Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
            <TextBlock Text="{x:Bind ViewModel.CurrentDate}" Style="{StaticResource SubtitleTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            
            <Grid Margin="0,24,0,0" ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                
                <Border Grid.Column="0" Background="{StaticResource CardBackgroundBrush}" CornerRadius="{StaticResource CardCornerRadius}" Padding="20" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Tarefas de Hoje" Style="{StaticResource BodyStrongTextBlockStyle}" Foreground="{StaticResource SystemControlHighlightAccentBrush}" />
                        <TextBlock Text="3 pendentes" Margin="0,8,0,0" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" Background="{StaticResource CardBackgroundBrush}" CornerRadius="{StaticResource CardCornerRadius}" Padding="20" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Tempo de Foco" Style="{StaticResource BodyStrongTextBlockStyle}" Foreground="{StaticResource SystemControlHighlightAccentBrush}" />
                        <TextBlock Text="1h 20m hoje" Margin="0,8,0,0" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </Grid>
</Page>

