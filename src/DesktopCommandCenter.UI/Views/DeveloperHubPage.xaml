<Page
    x:Class="DesktopCommandCenter.UI.Views.DeveloperHubPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:DesktopCommandCenter.UI.Controls"
    xmlns:local="using:DesktopCommandCenter.UI.Views"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Spacing="8" Margin="0,0,0,24">
            <TextBlock x:Uid="DevHubTitle" Text="Developer Hub" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock x:Uid="DevHubSubtitle" Text="Gerencie todas as suas linguagens, SDKs, CLIs e ferramentas locais em um só lugar." 
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <Pivot Grid.Row="1">
            <PivotItem x:Uid="DevHubTabSDKs" Header="SDKs &amp; Linguagens">
                <ScrollViewer>
                    <StackPanel Spacing="16" Padding="0,16,0,0">
                        <TextBlock x:Uid="DevHubLangDetected" Text="Linguagens Detectadas" Style="{StaticResource SubtitleTextBlockStyle}" />
                        
                        <controls:InfoCard Title=".NET 9 SDK" IconGlyph="&#xE943;">
                            <Grid>
                                <TextBlock Text="Instalado" HorizontalAlignment="Left" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <Button Content="Atualizar" HorizontalAlignment="Right" />
                            </Grid>
                        </controls:InfoCard>

                        <controls:InfoCard Title="Node.js" IconGlyph="&#xE943;">
                            <Grid>
                                <TextBlock Text="v20.10.0" HorizontalAlignment="Left" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <Button Content="Gerenciar via nvm" HorizontalAlignment="Right" />
                            </Grid>
                        </controls:InfoCard>
                        
                        <controls:InfoCard Title="Python" IconGlyph="&#xE943;">
                            <Grid>
                                <TextBlock Text="3.11.5" HorizontalAlignment="Left" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <Button Content="pip list" HorizontalAlignment="Right" />
                            </Grid>
                        </controls:InfoCard>
                    </StackPanel>
                </ScrollViewer>
            </PivotItem>
            
            <PivotItem x:Uid="DevHubTabCloud" Header="CLIs &amp; Cloud">
                <ScrollViewer>
                    <StackPanel Spacing="16" Padding="0,16,0,0">
                        <TextBlock x:Uid="DevHubCloudDetected" Text="Ferramentas de Nuvem Detectadas" Style="{StaticResource SubtitleTextBlockStyle}" />
                        <controls:InfoCard Title="Firebase CLI" IconGlyph="&#xE756;">
                            <Grid>
                                <TextBlock Text="Instalado via npm" HorizontalAlignment="Left" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="Pronto" HorizontalAlignment="Right" Foreground="{ThemeResource SystemFillColorSuccessBrush}"/>
                            </Grid>
                        </controls:InfoCard>
                        <controls:InfoCard Title="Vercel CLI" IconGlyph="&#xE756;">
                            <Grid>
                                <TextBlock Text="Instalado via npm" HorizontalAlignment="Left" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="Pronto" HorizontalAlignment="Right" Foreground="{ThemeResource SystemFillColorSuccessBrush}"/>
                            </Grid>
                        </controls:InfoCard>
                    </StackPanel>
                </ScrollViewer>
            </PivotItem>

            <PivotItem x:Uid="DevHubTabDB" Header="Containers &amp; Bancos">
                <ScrollViewer>
                    <StackPanel Spacing="16" Padding="0,16,0,0">
                        <TextBlock x:Uid="DevHubLocalServices" Text="Serviços Locais" Style="{StaticResource SubtitleTextBlockStyle}" />
                        <controls:InfoCard Title="Docker Desktop" IconGlyph="&#xECA5;">
                            <Grid>
                                <TextBlock Text="Engine v24.0.5" HorizontalAlignment="Left" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="Rodando" HorizontalAlignment="Right" Foreground="{ThemeResource SystemFillColorSuccessBrush}"/>
                            </Grid>
                        </controls:InfoCard>
                        <controls:InfoCard Title="PostgreSQL" IconGlyph="&#xE943;">
                            <Grid>
                                <TextBlock Text="Porta 5432" HorizontalAlignment="Left" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="Serviço Ativo" HorizontalAlignment="Right" Foreground="{ThemeResource SystemFillColorSuccessBrush}"/>
                            </Grid>
                        </controls:InfoCard>
                    </StackPanel>
                </ScrollViewer>
            </PivotItem>
            
            <PivotItem x:Uid="DevHubTabAI" Header="IAs Locais">
                <ScrollViewer>
                    <StackPanel Spacing="16" Padding="0,16,0,0">
                        <TextBlock x:Uid="DevHubAIModels" Text="Modelos e APIs de IA" Style="{StaticResource SubtitleTextBlockStyle}" />
                        <ItemsControl ItemsSource="{x:Bind LocalAIs}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="local:LocalAIStatus">
                                    <controls:InfoCard Title="{x:Bind Name}" IconGlyph="&#xEC19;" Margin="0,0,0,16">
                                        <Grid>
                                            <TextBlock Text="{x:Bind Endpoint}" HorizontalAlignment="Left" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                            <TextBlock Text="{x:Bind StatusText, Mode=OneWay}" HorizontalAlignment="Right" Foreground="{x:Bind StatusColor, Mode=OneWay}"/>
                                        </Grid>
                                    </controls:InfoCard>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>
