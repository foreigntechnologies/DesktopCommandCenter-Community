<Page
    x:Class="DesktopCommandCenter.UI.Views.DeveloperHubPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:DesktopCommandCenter.UI.Controls"
    xmlns:local="using:DesktopCommandCenter.UI.Views"
    xmlns:helpers="using:DesktopCommandCenter.UI.Helpers"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Spacing="8" Margin="0,0,0,24">
            <TextBlock Text="{x:Bind helpers:LocalizationHelper.GetLocalized('DevHubTitle.Text')}" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock Text="{x:Bind helpers:LocalizationHelper.GetLocalized('DevHubSubtitle.Text')}" 
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <Pivot Grid.Row="1">
            <PivotItem Header="{x:Bind helpers:LocalizationHelper.GetLocalized('DevHubTabSDKs.Header')}">
                <ScrollViewer>
                    <StackPanel Spacing="16" Padding="0,16,0,0">
                        <TextBlock Text="{x:Bind helpers:LocalizationHelper.GetLocalized('DevHubLangDetected.Text')}" Style="{StaticResource SubtitleTextBlockStyle}" />
                        
                        <controls:InfoCard Title=".NET 9 SDK" IconGlyph="&#xE943;">
                            <Grid>
                                <TextBlock Text="{x:Bind helpers:LocalizationHelper.GetLocalized('DevHub_Installed')}" HorizontalAlignment="Left" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <Button Content="{x:Bind helpers:LocalizationHelper.GetLocalized('DevHub_Update')}" HorizontalAlignment="Right" />
                            </Grid>
                        </controls:InfoCard>

                        <controls:InfoCard Title="Node.js" IconGlyph="&#xE943;">
                            <Grid>
                                <TextBlock Text="v20.10.0" HorizontalAlignment="Left" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <Button Content="{x:Bind helpers:LocalizationHelper.GetLocalized('DevHub_ManageNVM')}" HorizontalAlignment="Right" />
                            </Grid>
                        </controls:InfoCard>
                        
                        <controls:InfoCard Title="Python" IconGlyph="&#xE943;">
                            <Grid>
                                <TextBlock Text="3.11.5" HorizontalAlignment="Left" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <Button Content="{x:Bind helpers:LocalizationHelper.GetLocalized('DevHub_PipList')}" HorizontalAlignment="Right" />
                            </Grid>
                        </controls:InfoCard>
                    </StackPanel>
                </ScrollViewer>
            </PivotItem>
            
            <PivotItem Header="{x:Bind helpers:LocalizationHelper.GetLocalized('DevHubTabCloud.Header')}">
                <ScrollViewer>
                    <StackPanel Spacing="16" Padding="0,16,0,0">
                        <TextBlock Text="{x:Bind helpers:LocalizationHelper.GetLocalized('DevHubCloudDetected.Text')}" Style="{StaticResource SubtitleTextBlockStyle}" />
                        <controls:InfoCard Title="Firebase CLI" IconGlyph="&#xE756;">
                            <Grid>
                                <TextBlock Text="{x:Bind helpers:LocalizationHelper.GetLocalized('DevHub_InstalledNpm')}" HorizontalAlignment="Left" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{x:Bind helpers:LocalizationHelper.GetLocalized('DevHub_Update')}" HorizontalAlignment="Right" Foreground="{ThemeResource SystemFillColorSuccessBrush}"/>
                            </Grid>
                        </controls:InfoCard>
                        <controls:InfoCard Title="Vercel CLI" IconGlyph="&#xE756;">
                            <Grid>
                                <TextBlock Text="{x:Bind helpers:LocalizationHelper.GetLocalized('DevHub_InstalledNpm')}" HorizontalAlignment="Left" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{x:Bind helpers:LocalizationHelper.GetLocalized('DevHub_Update')}" HorizontalAlignment="Right" Foreground="{ThemeResource SystemFillColorSuccessBrush}"/>
                            </Grid>
                        </controls:InfoCard>
                    </StackPanel>
                </ScrollViewer>
            </PivotItem>

            <PivotItem Header="{x:Bind helpers:LocalizationHelper.GetLocalized('DevHubTabDB.Header')}">
                <ScrollViewer>
                    <StackPanel Spacing="16" Padding="0,16,0,0">
                        <TextBlock Text="{x:Bind helpers:LocalizationHelper.GetLocalized('DevHubLocalServices.Text')}" Style="{StaticResource SubtitleTextBlockStyle}" />
                        <controls:InfoCard Title="Docker Desktop" IconGlyph="&#xECA5;">
                            <Grid>
                                <TextBlock Text="{x:Bind helpers:LocalizationHelper.GetLocalized('DevHub_Installed')}" HorizontalAlignment="Left" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{x:Bind helpers:LocalizationHelper.GetLocalized('DevHub_Running')}" HorizontalAlignment="Right" Foreground="{ThemeResource SystemFillColorSuccessBrush}"/>
                            </Grid>
                        </controls:InfoCard>
                        <controls:InfoCard Title="PostgreSQL" IconGlyph="&#xE943;">
                            <Grid>
                                <TextBlock Text="{x:Bind helpers:LocalizationHelper.GetLocalized('DevHub_Port')}" HorizontalAlignment="Left" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{x:Bind helpers:LocalizationHelper.GetLocalized('DevHub_Running')}" HorizontalAlignment="Right" Foreground="{ThemeResource SystemFillColorSuccessBrush}"/>
                            </Grid>
                        </controls:InfoCard>
                    </StackPanel>
                </ScrollViewer>
            </PivotItem>
            
            <PivotItem Header="{x:Bind helpers:LocalizationHelper.GetLocalized('DevHubTabAI.Header')}">
                <ScrollViewer>
                    <StackPanel Spacing="16" Padding="0,16,0,0">
                        <TextBlock Text="{x:Bind helpers:LocalizationHelper.GetLocalized('DevHubAIModels.Text')}" Style="{StaticResource SubtitleTextBlockStyle}" />
                        <ItemsControl ItemsSource="{x:Bind LocalAIs}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="local:LocalAIStatus">
                                    <controls:InfoCard Title="{x:Bind Name}" IconGlyph="&#xEC19;" Margin="0,0,0,16">
                                        <Grid>
                                            <TextBlock Text="{x:Bind Endpoint}" HorizontalAlignment="Left" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                            <TextBlock Text="{x:Bind StatusText, Mode=OneWay}" HorizontalAlignment="Right" Foreground="{x:Bind StatusColor, Mode=OneWay}"/>
                                        </Grid>
                                    </controls:InfoCard>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>
