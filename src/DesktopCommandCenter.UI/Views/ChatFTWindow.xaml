<Window
    x:Class="DesktopCommandCenter.UI.Views.ChatFTWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel x:Name="AppTitleBar" MinHeight="40" VerticalAlignment="Top" Background="Transparent" Padding="12,0" Orientation="Horizontal">
            <Image Source="/Assets/ChatFT-LogoSemTexto.png" Width="16" Height="16" VerticalAlignment="Center" />
            <TextBlock Text="ChatFT" VerticalAlignment="Center" FontSize="12" Foreground="{ThemeResource TextFillColorPrimaryBrush}" Margin="8,0,0,0"/>
        </StackPanel>
        
        <Frame x:Name="RootFrame" Grid.Row="1" />
    </Grid>
</Window>
