<Page
    x:Class="DesktopCommandCenter.UI.Views.AutomacoesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:viewmodels="using:DesktopCommandCenter.UI.ViewModels"
    Background="Transparent">
    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,16">
            <TextBlock Text="Regras de Automação" Style="{StaticResource TitleTextBlockStyle}" />
            <Button Content="+ Nova Regra" Command="{x:Bind ViewModel.NovaRegraCommand}" HorizontalAlignment="Right" Style="{StaticResource AccentButtonStyle}" />
        </Grid>

        <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.Regras, Mode=OneWay}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="viewmodels:AutomacaoRegra">
                    <Grid Padding="16" Margin="0,4" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel>
                            <TextBlock Text="Se isso acontecer (Gatilho):" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Text="{x:Bind Gatilho, Mode=TwoWay}" FontWeight="Bold" FontSize="16" />
                        </StackPanel>

                        <FontIcon Grid.Column="1" Glyph="&#xE72A;" Margin="24,0" Foreground="{ThemeResource SystemAccentColor}" />

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Então faça isso (Ação):" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Text="{x:Bind Acao, Mode=TwoWay}" FontWeight="Bold" FontSize="16" />
                        </StackPanel>

                        <ToggleSwitch Grid.Column="3" IsOn="{x:Bind IsAtivo, Mode=TwoWay}" Margin="24,0,0,0" VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
