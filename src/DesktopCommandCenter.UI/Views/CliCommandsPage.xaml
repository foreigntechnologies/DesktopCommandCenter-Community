<Page NavigationCacheMode="Required"
    x:Class="DesktopCommandCenter.UI.Views.CliCommandsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:DesktopCommandCenter.UI.ViewModels"
    mc:Ignorable="d"
    x:Name="RootPage"
    Background="Transparent">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="CliPageTitleElement" Text="Comandos CLI" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,16"  />

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="CliInputSearchPlaceholderElement" PlaceholderText="Pesquisar comando, descrição ou título..." Text="{x:Bind ViewModel.SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,0,16,0"  />

            <ComboBox Grid.Column="1" 
                       ItemsSource="{x:Bind ViewModel.Categories, Mode=OneWay}" 
                       SelectedItem="{x:Bind ViewModel.SelectedCategory, Mode=TwoWay}"
                       HorizontalAlignment="Stretch" 
                       VerticalAlignment="Center" />
        </Grid>

        <ListView Grid.Row="2" ItemsSource="{x:Bind ViewModel.FilteredCommands, Mode=OneWay}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="viewmodels:CliCommandItem">
                    <Grid Padding="16" Margin="0,0,0,12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <Border Background="{ThemeResource AccentFillColorDefaultBrush}" CornerRadius="4" Padding="8,4" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="0,0,16,0">
                            <TextBlock Text="{x:Bind Category}" Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}" FontWeight="Bold" FontSize="11" TextTrimming="CharacterEllipsis"/>
                        </Border>

                        <StackPanel Grid.Column="1">
                            <TextBlock Text="{x:Bind Title}" FontWeight="Bold" Margin="0,0,0,4" />
                            <TextBlock Text="{x:Bind Description}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,8" />
                            <TextBox Text="{x:Bind Command}" IsReadOnly="True" FontFamily="Consolas" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" BorderThickness="0" Padding="8" CornerRadius="4" />
                        </StackPanel>

                        <!-- Botão Copiar -->
                        <Button Grid.Column="2" VerticalAlignment="Center" Margin="16,0,0,0"
                                Command="{Binding ElementName=RootPage, Path=ViewModel.CopyCommandCommand}"
                                CommandParameter="{x:Bind Command}">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <SymbolIcon Symbol="Copy" />
                                <TextBlock x:Name="CliBtnCopyElement" ToolTipService.ToolTip="{Binding Converter={StaticResource TranslateConverter}, ConverterParameter='Cli_BtnCopy'}" Text="Copiar"  />
                            </StackPanel>
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>


