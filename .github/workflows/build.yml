name: Build

on:
  push:
    branches: [ "master", "main", "enterprise" ]
  pull_request:
    branches: [ "master", "main", "enterprise" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 9.0.x

    - name: Restore dependencies
      # -r win-x64 garante que os runtime packs do WinUI sejam baixados
      # antes do build com --no-restore
      run: dotnet restore DesktopCommandCenter.slnx -r win-x64

    - name: Build
      run: dotnet build DesktopCommandCenter.slnx --no-restore --configuration Release -r win-x64
