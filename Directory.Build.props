<Project>
  <!-- Import the root Directory.Build.props from the Enterprise parent directory so all projects share the same BaseOutputPath and Platforms -->
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />
</Project>
